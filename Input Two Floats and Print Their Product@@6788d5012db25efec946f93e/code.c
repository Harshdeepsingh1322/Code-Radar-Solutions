#include <stdio.h>

int main() {
    float a, b;
    
    // Take two floating-point numbers as input
    scanf("%f %f", &a, &b);
    
    // Print their product
    printf("Product: %.2f\n", a * b);
    
    return 0;
}
