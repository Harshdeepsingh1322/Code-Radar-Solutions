#include <stdio.h>

int main() {
    char ch;
    // Taking a single character as input
    scanf("%c", &ch);
    // Printing the entered character
    printf("You entered: %c\n", ch);
    return 0;
}
