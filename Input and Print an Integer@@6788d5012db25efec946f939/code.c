#include <stdio.h>

int main() {
    char str[100];
    // Taking a string of characters as input
    scanf("%s", str);
    // Printing the entered string
    printf("You entered: %s\n", str);
    return 0;
}
